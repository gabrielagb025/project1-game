/*class Obstacle {
    constructor(ctx, x, y, width, height, color, vy) {
        this.ctx = ctx;
        this.x = x
        this.y = y;
        this.width = width;
        this.height = height;
        this.color = color;
        this.vy = vy;
      }

      /*constructor(ctx, x, y, imageSrc, vy) {
        this.ctx = ctx;
        this.x = x
        this.y = y;

        this.image = new Image();
        this.image.src = imageSrc;
        this.image.onload = () => {
          this.isReady = true;
        }

        this.vy = vy;
      }

    draw() {
        this.ctx.fillStyle = this.color;
        this.ctx.fillRect(this.x, this.y, this.width, this.height);

        /*if (this.isReady) {
          this.ctx.drawImage(
            this.image,
            this.x,
            this.y,
            25,
            35
          )
        }
      }

    move() {
        this.y += this.vy;
    }
}*/